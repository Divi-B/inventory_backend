# Mapping which features are needed for each model
FEATURE_MAPPING = {
    "Target Market": ["feature1", "feature2", "feature3"],
    "Sales per Customer": ["featureA", "featureB", "featureC"],
    # Add more types here
}
